import React from 'react'
import styles from './AddMarker.module.scss';
const AddIcon = (isAddIcon, ) => {

  return (
    isAddIcon && (
        <div>AddIcon</div>
    )
  )
}

export default AddIcon